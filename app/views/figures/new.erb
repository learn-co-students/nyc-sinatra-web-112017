<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <form  action="/figures" method="post">
      Figure Name: <input id="figure_name" type="text" name="figure[name]" value="figure name">
      <!-- Figure TitleIDs: <input id=<%-"title_#{}"%> type="text" name="figure[title_ids][]">
     -->
     Titles
      <% @titles.each do |title| %>
      Titles:  <input type="checkbox" name="figure[title_ids][]" value='<%= title.id %>' id="title_<%= title.id %>">
      <%= title.name %>
      <% end %>
      Title Name: <input type="text" name="title[name]" value="">
      Figure LandmarkIDs: <input type="text" name="figure[landmark_ids][]">
      Landmark Name: <input type="text" name="landmark[name]">
      <input type="submit" value="Submit" id="Submit">
    </form>
  </body>
</html>
