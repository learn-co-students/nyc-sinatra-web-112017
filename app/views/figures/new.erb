<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <form action="/figures" method="post">

    <p>Name<input type="text" name="figure[name]" id="figure_name"></p>

    <% @landmarks.each do |lm| %>
    <input type="checkbox" value="<%=lm.id%>" name="figure[landmark_ids][]" id="landmark_<%=lm.id%>"><%=lm.name%></input>
    <%end%>
    <br></br>

    <% @titles.each do |t| %>
    <input type="checkbox" value="<%=t.id%>" name="figure[title_ids][]" id="title_<%=t.id%>"><%=t.name%></input>
    <%end%>
    <br></br>

    <input type="text" name="landmark[name]" id="new_landmark">
    <input type="text" name="title[name]" id="new_title">

    <input type="submit" value="Create New Figure">

    </form>

  </body>
</html>
