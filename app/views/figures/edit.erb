<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <form action="/figures/<%=@figure.id %>" method="post">
      <input type="hidden" id="hidden" name="_method" value="patch">
      <h3>Figure</h3>
      <p><input type="text" id="figure_name" name="figure[name]" value="<%= @figure.name%>"</p>
      <h4>Titles</h4>
      <% @titles.each do |title| %>
        <% if @figure.titles.include?(title) %>
          <p><%= title.name %><input type="checkbox"  name="figure[title_ids][]" value="<%= title.id%>" checked></p>
        <% else %>
        <p><%= title.name %><input type="checkbox"  name="figure[title_ids][]" value="<%= title.id%>"></p>
        <% end %>
      <% end %>
      <p> Title Name: <input type="text" name="title[name]" id="new_title"> </p>
      <h4>Landmarks</h4>
      <% @landmarks.each do |landmark| %>
        <% if @figure.landmarks.include?(landmark) %>
          <p><%= landmark.name %><input type="checkbox"  name="figure[landmark_ids][]" value="<%= landmark.id%>" checked></p>
        <% else %>
        <p><%= landmark.name %><input type="checkbox"  name="figure[landmark_ids][]" value="<%= landmark.id%>"></p>
        <% end %>
      <% end %>
      <p> Landmark Name: <input type="text" name="landmark[name]" id="new_landmark"></p>
      <button type="submit" value="submit">Edit Figure</button>
    </form>
  </body>
</html>
